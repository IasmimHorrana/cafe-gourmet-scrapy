name: 'dbt_coffee'
version: '1.0.0'

profile: 'dbt_coffee'  

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

vars:
    "dbt_date:time_zone": "America/Sao_Paulo"
    source_schema: "raw"  
    source_database: "cafe_db"  

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_coffee:
    staging:
      +materialized: view
    
    intermediate:
      +materialized: table
    
    mart:
      +materialized: table